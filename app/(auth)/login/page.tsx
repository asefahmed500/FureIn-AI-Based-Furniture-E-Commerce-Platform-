import { LoginForm } from "@/components/auth/login-form"

export const metadata = {
  title: "Secure Entry | FureIn",
  description: "Access your curated architectural collection.",
}

export default function LoginPage() {
  return <LoginForm />
}
