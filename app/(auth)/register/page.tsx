import { RegisterForm } from "@/components/auth/register-form"

export const metadata = {
  title: "Citizenship Request | FureIn",
  description: "Join our guild of architectural collectors.",
}

export default function RegisterPage() {
  return <RegisterForm />
}
